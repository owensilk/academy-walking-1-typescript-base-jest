const board = () => {
    let grid = [[null, null, null], [null, null, null], [null, null, null]]
    const state = () => {
        return grid
    }

    const set = () => {

    }

    return { state, set };
};

export default board;
